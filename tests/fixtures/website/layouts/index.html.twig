{% include 'include/header.html.twig' %}

All pages:
<ul>
    {% for item in site.pages|sortByDate %}
        <li>
            <a href="{{ url(item) }}">{{ item.title }}</a> ({{ item.id }} - {{ item.date|date("d/m/Y") }})
        </li>
    {% endfor %}
</ul>

{% include 'include/footer.html.twig' %}
