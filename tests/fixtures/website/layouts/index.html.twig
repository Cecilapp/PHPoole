{% include 'include/header.html.twig' %}

<p>Pages:</p>
<ul>
    {% for item in site.pages|filterBy('section', 'blog')|sortByDate %}
    <li>
        <a href="{{ url(item) }}">{{ item.title }}</a>
        {{ item.content|excerpt }}
    </li>
    {% endfor %}
</ul>

<p>layout: {{ _self.getTemplateName() }}</p>
{% include 'include/footer.html.twig' %}
